import type { Tag } from './Tags'

/**
 * Props for series card components (grid and list variants).
 * Shared interface used by SeriesGridCard and SeriesListCard components.
 */
export interface SeriesCardProps {
  tag: Tag
}
